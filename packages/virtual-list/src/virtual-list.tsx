import { component, useStore } from "@traxjs/trax-react";
import './virtual-list.css';

export const VirtualList = component("VirtualList", () => {
    // const store = useStore(createVirtualListStore);
    // const data = store.data;

    return <div className="virtual-list">
        [Under construction]
    </div>
});
